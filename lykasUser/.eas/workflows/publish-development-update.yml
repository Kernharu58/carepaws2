name: Publish preview update

on:
  push:
    branches:
      - main

jobs:
  publish_preview_update:
    name: Publish preview update
    type: update
    environment: preview
    params:
      channel: preview
      message: "Preview update"
      platform: android